# Tests

This directory contains unit tests verifying:

- Layer forward/backward propagation
- Shape correctness
- Optimizer updates
- Loss implementations
- Model inference

Run:

```bash
python tests/run_tests.py
```

Each test file prints `OK` on success.